def func(x: object, y: object = 50) -> object:
    print("x=", x)
    print("y=", y)


func(50)

# types of function arguments 1 default arguments it is a parameter that assumes a default value if a value is not
# provided in the function call Default arguments have any number of arguments in a functions as a default values
# keyword arguments it allows the collar to specify the argument name with values so that collar does not need to
# --remember the order of parameters
# dog string:
# the first string after the function is called dog string.it is used to describe the functionality
# syntax:print(func_name._doc_)

